spring:
  # ?????? ??
  application:
    name: user-service

  # ????? (MySQL) ??
  datasource:
    url: jdbc:mysql://mysql_db:3306/user?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate ??
  jpa:
    hibernate:
      ddl-auto: update  # ??? ?? ??/???? (???)
    show-sql: true      # SQL ?? ?? ??
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect  # MySQL ??

  # Redis ??
  data:
    redis:
      host: redis_cache
      port: 6379

# ?? ?? ??
server:
  port: 8080